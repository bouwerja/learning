| Keyword / Reserved Word | Description / Category |
| :--- | :--- |
| `abstract` | Reserved / Future Reserved Word |
| `arguments` | Special variable inside function bodies |
| `await` | Reserved in async functions / modules |
| `boolean` | Reserved / Future Reserved Word |
| `break` | Terminates a loop or switch statement |
| `byte` | Reserved / Future Reserved Word |
| `case` | Marks a clause in a switch statement |
| `catch` | Handles errors in a try block |
| `char` | Reserved / Future Reserved Word |
| `class` | Declares a class |
| `const` | Declares a block-scoped constant |
| `continue` | Skips to the next iteration in a loop |
| `debugger` | Invokes debugging functions |
| `default` | Specifies the default case in a switch statement |
| `delete` | Deletes a property from an object |
| `do` | Starts a do...while loop |
| `double` | Reserved / Future Reserved Word |
| `else` | Specifies alternative branch in an if statement |
| `enum` | Reserved / Future Reserved Word |
| `eval` | Global function / Reserved in strict mode |
| `export` | Exports bindings from a module |
| `extends` | Inherits a class |
| `false` | Boolean literal |
| `final` | Reserved / Future Reserved Word |
| `finally` | Executes code after try/catch regardless of outcome |
| `float` | Reserved / Future Reserved Word |
| `for` | Starts a for loop |
| `function` | Declares a function |
| `goto` | Reserved / Future Reserved Word |
| `if` | Executes code based on a condition |
| `implements` | Reserved in strict mode / Future Reserved Word |
| `import` | Imports modules |
| `in` | Checks if a property exists in an object |
| `instanceof` | Checks if an object is an instance of a constructor |
| `int` | Reserved / Future Reserved Word |
| `interface` | Reserved in strict mode / Future Reserved Word |
| `let` | Declares a block-scoped variable |
| `long` | Reserved / Future Reserved Word |
| `native` | Reserved / Future Reserved Word |
| `new` | Creates an instance of a constructor |
| `null` | Primitive value representing intentional absence |
| `package` | Reserved in strict mode / Future Reserved Word |
| `private` | Reserved in strict mode / Future Reserved Word |
| `protected` | Reserved in strict mode / Future Reserved Word |
| `public` | Reserved in strict mode / Future Reserved Word |
| `return` | Returns a value from a function |
| `short` | Reserved / Future Reserved Word |
| `static` | Defines static methods or properties in a class |
| `super` | Calls parent constructor or methods |
| `switch` | Evaluates an expression against multiple cases |
| `synchronized` | Reserved / Future Reserved Word |
| `this` | Refers to the current execution context |
| `throw` | Throws a user-defined exception |
| `throws` | Reserved / Future Reserved Word |
| `transient` | Reserved / Future Reserved Word |
| `true` | Boolean literal |
| `try` | Defines a block of code to test for errors |
| `typeof` | Returns the data type of an expression |
| `var` | Declares a function-scoped variable |
| `void` | Evaluates an expression and returns undefined |
| `volatile` | Reserved / Future Reserved Word |
| `while` | Starts a while loop |
| `with` | Extends the scope chain for a statement (deprecated) |
| `yield` | Pauses and resumes a generator function |