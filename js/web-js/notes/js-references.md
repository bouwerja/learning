# JavaScript Alphabetic Reference

| Name | Type / Kind | Description |
| :--- | :--- | :--- |
| `abs()` | Math Method | Returns the absolute value of a number |
| `acos()` | Math Method | Returns the arccosine of a number in radians |
| `acosh()` | Math Method | Returns the hyperbolic arccosine of a number |
| `addEventListener()` | Event Method | Attaches an event handler to an element |
| `alert()` | Window Method | Displays an alert box with a message and an OK button |
| `all()` | Promise Method | Resolves when all promises in an iterable resolve |
| `allSettled()` | Promise Method | Resolves when all promises in an iterable have settled |
| `any()` | Promise Method | Resolves as soon as any promise in an iterable resolves |
| `anchor()` | String Method | (Deprecated) Creates an HTML anchor tag |
| `append()` | Node/FormData Method | Appends data or nodes to a parent/container |
| `appendChild()` | Node Method | Adds a new child node to an element |
| `apply()` | Function Method | Calls a function with a given `this` value and arguments provided as an array |
| `Array` | Global Object | Constructor for creating Array objects |
| `Array.from()` | Array Method | Creates an array from an array-like or iterable object |
| `Array.isArray()` | Array Method | Determines whether the passed value is an Array |
| `Array.of()` | Array Method | Creates a new Array instance from a variable number of arguments |
| `asin()` | Math Method | Returns the arcsine of a number in radians |
| `asinh()` | Math Method | Returns the hyperbolic arcsine of a number |
| `assign()` | Object Method | Copies all enumerable own properties from source objects to a target object |
| `async` | Keyword | Defines an asynchronous function |
| `atan()` | Math Method | Returns the arctangent of a number in radians |
| `atan2()` | Math Method | Returns the arctangent of the quotient of its arguments |
| `atanh()` | Math Method | Returns the hyperbolic arctangent of a number |
| `await` | Keyword | Pauses execution of an async function until a Promise settles |
| `big()` | String Method | (Deprecated) Creates a `<big>` HTML element |
| `BigInt` | Global Object | Constructor for large integer values |
| `bind()` | Function Method | Creates a new function with a bound `this` value |
| `blink()` | String Method | (Deprecated) Creates a `<blink>` HTML element |
| `bold()` | String Method | (Deprecated) Creates a `<b>` HTML element |
| `Boolean` | Global Object | Object wrapper for boolean values |
| `break` | Statement | Exits a loop or switch statement |
| `call()` | Function Method | Calls a function with a given `this` value and arguments provided individually |
| `case` | Keyword | Specifies a block of code in a `switch` statement |
| `catch` | Statement | Handles errors in `try...catch` blocks |
| `cbrt()` | Math Method | Returns the cube root of a number |
| `ceil()` | Math Method | Rounds a number upward to the nearest integer |
| `charAt()` | String Method | Returns the character at a specified index |
| `charCodeAt()` | String Method | Returns the Unicode of the character at a specified index |
| `codePointAt()` | String Method | Returns a non-negative integer that is the UTF-16 code point value |
| `class` | Keyword | Declares a class in JavaScript |
| `clear()` | Map/Set/Console Method | Clears contents of a Map, Set, or Console |
| `clearInterval()` | Window Method | Clears a timer set with `setInterval()` |
| `clearTimeout()` | Window Method | Clears a timer set with `setTimeout()` |
| `cloneNode()` | Node Method | Clones an HTML element |
| `clz32()` | Math Method | Returns the number of leading zero bits in a 32-bit binary representation |
| `concat()` | Array/String Method | Joins two or more arrays or strings |
| `confirm()` | Window Method | Displays a dialog box with a message, OK, and Cancel buttons |
| `console` | Global Object | Provides access to the browser's debugging console |
| `const` | Keyword | Declares a read-only, block-scoped constant variable |
| `continue` | Statement | Breaks one iteration in a loop and continues with the next |
| `copyWithin()` | Array Method | Copies array elements within the array to/from specified positions |
| `cos()` | Math Method | Returns the cosine of a number |
| `cosh()` | Math Method | Returns the hyperbolic cosine of a number |
| `create()` | Object Method | Creates a new object with the specified prototype object and properties |
| `createElement()` | Document Method | Creates an Element node |
| `debugger` | Statement | Stops the execution of JavaScript and calls the debugging function |
| `decodeURI()` | Global Method | Decodes a Uniform Resource Identifier (URI) |
| `decodeURIComponent()` | Global Method | Decodes a URI component |
| `default` | Keyword | Specifies the default code block in a `switch` statement |
| `defineProperties()` | Object Method | Defines new or modifies existing properties on an object |
| `defineProperty()` | Object Method | Defines a new property directly on an object |
| `delete` | Operator | Deletes a property from an object |
| `do...while` | Statement | Executes a statement block once, and then repeats while a condition is true |
| `document` | Global Object | Represents the web page loaded in the browser |
| `encodeURI()` | Global Method | Encodes a Uniform Resource Identifier (URI) |
| `encodeURIComponent()` | Global Method | Encodes a URI component |
| `endsWith()` | String Method | Checks whether a string ends with specified characters |
| `entries()` | Array/Map/Set Method | Returns an Iterator with key/value pairs |
| `Error` | Global Object | Base object for handling runtime errors |
| `eval()` | Global Method | Evaluates JavaScript code represented as a string |
| `every()` | Array Method | Checks if every element in an array passes a test |
| `exp()` | Math Method | Returns $e^x$, where $x$ is the argument |
| `export` | Keyword | Exports functions/modules from a file |
| `extends` | Keyword | Extends a class (subclassing) |
| `expm1()` | Math Method | Returns $e^x - 1$ |
| `fetch()` | Global Method | Starts the process of fetching a resource from the network |
| `fill()` | Array Method | Fills elements in an array with a static value |
| `filter()` | Array Method | Creates a new array with elements that pass a test condition |
| `find()` | Array Method | Returns the value of the first element in an array that satisfies a test |
| `findIndex()` | Array Method | Returns the index of the first element in an array that satisfies a test |
| `findLast()` | Array Method | Returns the value of the last element in an array that satisfies a test |
| `findLastIndex()` | Array Method | Returns the index of the last element in an array that satisfies a test |
| `finally` | Statement | Executes code after `try` and `catch` regardless of the result |
| `flat()` | Array Method | Flattens sub-array elements up to a specified depth |
| `flatMap()` | Array Method | Maps each element using a mapping function, then flattens the result |
| `floor()` | Math Method | Rounds a number downward to the nearest integer |
| `fround()` | Math Method | Returns the nearest 32-bit single precision float representation of a number |
| `for` | Statement | Creates a loop executed while a condition evaluates to true |
| `for...in` | Statement | Loops through the properties of an object |
| `for...of` | Statement | Loops through the values of an iterable object |
| `forEach()` | Array/Map/Set Method | Calls a function for each element |
| `freeze()` | Object Method | Freezes an object (prevents new properties and modifications) |
| `fromCodePoint()` | String Method | Returns a string created by using a specified sequence of code points |
| `fromCharCode()` | String Method | Converts Unicode values into characters |
| `function` | Keyword | Declares a new function |
| `get` | Keyword | Binds an object property to a function that will be called when queried |
| `getDate()` | Date Method | Returns the day of the month (1-31) |
| `getDay()` | Date Method | Returns the day of the week (0-6) |
| `getElementById()` | Document Method | Returns the element that has the ID attribute with the specified value |
| `getElementsByClassName()` | Document Method | Returns a collection of all elements with a specified class name |
| `getElementsByTagName()` | Document Method | Returns a collection of all elements with a specified tag name |
| `getFullYear()` | Date Method | Returns the four-digit year |
| `getHours()` | Date Method | Returns the hour (0-23) |
| `getMilliseconds()` | Date Method | Returns the milliseconds (0-999) |
| `getMinutes()` | Date Method | Returns the minutes (0-59) |
| `getMonth()` | Date Method | Returns the month (0-11) |
| `getOwnPropertyNames()` | Object Method | Returns an array of all properties found directly in a given object |
| `getOwnPropertySymbols()` | Object Method | Returns an array of all symbol properties found directly on a given object |
| `getPrototypeOf()` | Object Method | Returns the prototype of the specified object |
| `getSeconds()` | Date Method | Returns the seconds (0-59) |
| `getTime()` | Date Method | Returns time in milliseconds since January 1, 1970 |
| `has()` | Map/Set Method | Returns a boolean indicating whether an element exists |
| `hasOwnProperty()` | Object Method | Returns a boolean indicating whether the object has the specified property |
| `hypot()` | Math Method | Returns the square root of the sum of squares of its arguments |
| `if...else` | Statement | Executes a block of code if a specified condition is true/false |
| `imul()` | Math Method | Returns the result of a 32-bit integer multiplication |
| `import` | Keyword | Imports functions/modules from an external file |
| `in` | Operator | Checks if a specified property exists in an object |
| `includes()` | Array/String Method | Checks whether an array/string contains a specified element/substring |
| `indexOf()` | Array/String Method | Returns the index of the first occurrence of a value |
| `instanceof` | Operator | Tests whether an object has in its prototype chain the constructor |
| `is()` | Object Method | Determines whether two values are the same value |
| `isExtensible()` | Object Method | Determines if an object is extensible (whether it can have new properties added) |
| `isFinite()` | Global/Number Method | Determines whether a value is a finite number |
| `isFrozen()` | Object Method | Determines if an object is frozen |
| `isNaN()` | Global/Number Method | Determines whether a value is NaN (Not-a-Number) |
| `isSealed()` | Object Method | Determines if an object is sealed |
| `join()` | Array Method | Joins all elements of an array into a string |
| `JSON.parse()` | JSON Method | Parses a JSON string and constructs the JavaScript value or object |
| `JSON.stringify()` | JSON Method | Converts a JavaScript value/object to a JSON string |
| `keys()` | Array/Map/Object/Set Method | Returns an iterator/array containing keys |
| `lastIndexOf()` | Array/String Method | Returns the index of the last occurrence of a value |
| `length` | Property | Returns length of array, string, or number of arguments expected by a function |
| `let` | Keyword | Declares a block-scoped local variable |
| `localeCompare()` | String Method | Compares two strings in the current locale |
| `log()` | Math/Console Method | Returns the natural logarithm of a number / Logs message to console |
| `log10()` | Math Method | Returns the base-10 logarithm of a number |
| `log1p()` | Math Method | Returns the natural logarithm of $1 + x$ |
| `log2()` | Math Method | Returns the base-2 logarithm of a number |
| `Map` | Global Object | Constructor for key-value collection objects |
| `map()` | Array Method | Creates a new array populated with the results of calling a function |
| `match()` | String Method | Searches a string for a match against a regular expression |
| `matchAll()` | String Method | Returns an iterator of all results matching a string against a regex |
| `Math` | Global Object | Built-in object that has properties and methods for mathematical constants and functions |
| `max()` | Math Method | Returns the number with the highest value |
| `min()` | Math Method | Returns the number with the lowest value |
| `new` | Operator | Creates an instance of a user-defined object type or built-in object |
| `Number` | Global Object | Object wrapper for numeric values |
| `Object` | Global Object | Base constructor object in JavaScript |
| `padEnd()` | String Method | Pads the end of a string with another string until target length |
| `padStart()` | String Method | Pads the start of a string with another string until target length |
| `parseFloat()` | Global Method | Parses a string argument and returns a floating point number |
| `parseInt()` | Global Method | Parses a string argument and returns an integer |
| `pop()` | Array Method | Removes the last element from an array and returns it |
| `pow()` | Math Method | Returns the value of $x$ to the power of $y$ ($x^y$) |
| `Promise` | Global Object | Represents the eventual completion or failure of an asynchronous operation |
| `prompt()` | Window Method | Displays a dialog box that prompts the visitor for input |
| `push()` | Array Method | Adds new elements to the end of an array and returns the new length |
| `querySelector()` | Document/Element Method | Returns the first element that matches a specified CSS selector |
| `querySelectorAll()` | Document/Element Method | Returns a static NodeList of all elements that match a specified CSS selector |
| `random()` | Math Method | Returns a pseudo-random number between 0 and 1 |
| `reduce()` | Array Method | Executes a reducer function on each array element, resulting in a single output value |
| `reduceRight()` | Array Method | Executes a reducer function on each array element from right-to-left |
| `RegExp` | Global Object | Constructor for creating regular expression objects |
| `repeat()` | String Method | Returns a new string with a specified number of copies of the string |
| `replace()` | String Method | Searches a string for a value and returns a new string where values are replaced |
| `replaceAll()` | String Method | Returns a new string with all matches of a pattern replaced |
| `return` | Statement | Exits a function and specifies the value to be returned |
| `reverse()` | Array Method | Reverses an array in place |
| `round()` | Math Method | Rounds a number to the nearest integer |
| `search()` | String Method | Searches a string for a specified value and returns the position of the match |
| `Set` | Global Object | Constructor for collection of unique values |
| `setDate()` | Date Method | Sets the day of the month for a specified date |
| `setFullYear()` | Date Method | Sets the full year for a specified date |
| `setHours()` | Date Method | Sets the hour for a specified date |
| `setInterval()` | Window Method | Calls a function or evaluates an expression at specified intervals |
| `setMinutes()` | Date Method | Sets the minutes for a specified date |
| `setMonth()` | Date Method | Sets the month for a specified date |
| `setSeconds()` | Date Method | Sets the seconds for a specified date |
| `setTime()` | Date Method | Sets a date to a specified time in milliseconds since Jan 1, 1970 |
| `setTimeout()` | Window Method | Calls a function or evaluates an expression after a specified delay |
| `shift()` | Array Method | Removes the first element from an array and returns it |
| `sign()` | Math Method | Returns the sign of a number (indicating whether it is positive or negative) |
| `sin()` | Math Method | Returns the sine of a number |
| `sinh()` | Math Method | Returns the hyperbolic sine of a number |
| `slice()` | Array/String Method | Extracts a section of an array/string and returns it as a new array/string |
| `some()` | Array Method | Checks if at least one element in an array passes a test condition |
| `sort()` | Array Method | Sorts the elements of an array in place |
| `splice()` | Array Method | Adds or removes elements from an array |
| `split()` | String Method | Splits a string into an array of substrings |
| `sqrt()` | Math Method | Returns the square root of a number |
| `startsWith()` | String Method | Checks whether a string begins with specified characters |
| `String` | Global Object | Constructor for creating string values |
| `substr()` | String Method | (Deprecated) Extracts characters from a string beginning at specified location |
| `substring()` | String Method | Extracts characters from a string between two specified indices |
| `super` | Keyword | Used to access and call functions on an object's parent |
| `switch` | Statement | Evaluates an expression against multiple `case` blocks |
| `Symbol` | Global Object | Constructor for unique, immutable primitive values |
| `tan()` | Math Method | Returns the tangent of a number |
| `tanh()` | Math Method | Returns the hyperbolic tangent of a number |
| `then()` | Promise Method | Appends fulfillment and rejection handlers to a promise |
| `this` | Keyword | Refers to the object to which a function/code block belongs |
| `throw` | Statement | Throws a user-defined exception |
| `toLowerCase()` | String Method | Converts a string to lowercase letters |
| `toString()` | Array/Number/Object Method | Converts an object to a string representation |
| `toUpperCase()` | String Method | Converts a string to uppercase letters |
| `trim()` | String Method | Removes whitespace from both ends of a string |
| `trimEnd()` | String Method | Removes whitespace from the end of a string |
| `trimStart()` | String Method | Removes whitespace from the start of a string |
| `try` | Statement | Defines a block of code to test for errors while being executed |
| `trunc()` | Math Method | Returns the integer part of a number by removing any fractional digits |
| `typeof` | Operator | Returns a string indicating the type of an unevaluated operand |
| `unshift()` | Array Method | Adds new elements to the beginning of an array and returns the new length |
| `values()` | Array/Map/Set Method | Returns an iterator object containing values |
| `var` | Keyword | Declares a variable, optionally initializing it to a value |
| `void` | Operator | Evaluates an expression and returns `undefined` |
| `WeakMap` | Global Object | Collection of key/value pairs in which keys are weakly referenced objects |
| `WeakSet` | Global Object | Collection of weakly held objects |
| `while` | Statement | Creates a loop that executes a specified statement as long as the test condition is true |
| `with` | Statement | (Deprecated) Extends the scope chain for a statement |
| `yield` | Keyword | Pauses and resumes a generator function |